*tiny-templates.nvim.txt*      For neovim-0.12     Last change: 2025 August 19

==============================================================================
Table of Contents                      *tiny-templates.nvim-table-of-contents*

1. tiny.nvim templates support|tiny-templates.nvim-tiny.nvim-templates-support|
  - Setup and Configuration |tiny-templates.nvim-tiny.nvim-templates-support-setup-and-configuration-|
  - Usage             |tiny-templates.nvim-tiny.nvim-templates-support-usage-|

==============================================================================
1. tiny.nvim templates support*tiny-templates.nvim-tiny.nvim-templates-support*

Tiny module to support template usage


SETUP AND CONFIGURATION *tiny-templates.nvim-tiny.nvim-templates-support-setup-and-configuration-*

Setupwith Lazy

>lua
    return {
        "Coding4Glory/tiny-templates.nvim",
        -- default options, pass an empty table to use defaults
        opts = {
            include = {
                vim.fs.joinpath(vim.fn.stdpath('config'), 'templates'),
                vim.fs.joinpath(vim.fn.stdpath('data'), 'lazy', 'tiny-templates.nvim', 'templates')
            },
            templates = {
                ['*.lua'] = 'skel.lua',
                ['ftplugin/*.vim'] = 'ftplugin.vim',
            },
            no_autoload = false
        }
    }
<


USAGE                 *tiny-templates.nvim-tiny.nvim-templates-support-usage-*


TEMPLATEFILES ~

If not changed via options place your templates into the template directory,
default on linux is `~/.config/nvim/templates`. Other OS place accordingly to
`vim.fs.joinpath(vim.fn.stdpath('config'), 'templates')`. The order of
directories specified in the include option is important. Only the first found
template will be loaded. This way multiple folders can be used for fallback or
override. The plugin contains two example templates.

- ftplugin.vim: a template for filetype plugins
- skel.lua: a template for lua modules


COMMANDS  ~

                                                                *TemplateApply*
:TemplateApply[!] {arg}      Fills the buffer with the template. If the buffer
                             already contains more than one line, the command
                             will do nothing. Use bang to clear the file in
                             advance which will override the buffer with the
                             template content. WARNING if the template is not
                             found, the buffer will still be cleared.

                                                                  *TemplateAdd*
:TemplateAdd {arg}           Adds the contents of the template at the cursor
                             line.

Both commands require the same argument with is either the pattern used to
match the new file or the simple file name of the template, examples based on
default configuration:

    :TemplateApply! ftplugin.vim
    :TemplateAdd *.lua

Generated by panvimdoc <https://github.com/kdheepak/panvimdoc>

vim:tw=78:ts=8:noet:ft=help:norl:
